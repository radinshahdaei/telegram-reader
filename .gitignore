# Secrets — never commit these
config.json
session.session

# Python
__pycache__/
*.pyc

# Fetched messages may be private — ignore by default
data/

# Local analysis output (personal — not part of the repo)
analysis.md

# macOS
.DS_Store
